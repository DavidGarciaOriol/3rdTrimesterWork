public class Athentication extends Middleware {


    public boolean authenticate(Request request){
        // if user exists and password hash matches, user is authenticated.
        return false;
    }
}